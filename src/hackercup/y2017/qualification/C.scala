package hackercup.y2017.qualification

import lib.CodeJam

/**
  * Created by essis on 2016. 1. 8..
  */
class C(fileName: String) extends CodeJam(fileName) {

  def solve(): Unit = {
    val row = br.readLine.split("\\s+").map(_.toInt)
    val (h, s) = (row(0), row(1))
    val srow = br.readLine.split("\\s+")

  }


  def init(): Unit = {

  }
}